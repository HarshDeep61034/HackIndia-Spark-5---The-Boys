/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'github.com', 'lh3.googleusercontent.com'],
      },
};

export default nextConfig;
